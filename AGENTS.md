# ZhijiLab 高保真原型工程约定

## 工程定位

- 本仓库是可独立运行、可直接评审的管理端高保真原型工程，Git 仓库是主要交付物。
- 开发或修改原型页面时，使用仓库内置的 `$build-zhijilab-prototype` Skill 配合完成需求澄清、产品与交互设计、页面实现和验收。
- 本仓库当前代码、依赖版本、公共组件、布局、样式、Design Token 和素材是唯一技术与设计基准。
- 不依赖、连接、同步或推断任何生产项目，不建立 baseline、快照同步或兼容机制。需要复用的版本与组件由维护者直接放入本仓库。

## 四项核心原则

- **真实组件**：优先复用仓库已有业务组件；其次使用仓库当前版本的 TDesign、ECharts 等组件。已有组件能完成的界面，禁止用原生元素和自绘样式模拟。
- **真实约束**：严格遵循仓库现有依赖、Design Token、布局、间距、字体、颜色、图标、交互模式和设计语言，不自行引入另一套设计系统。
- **真实交互**：需求定义的操作必须可以实际点击、输入、选择、切换、提交或反馈，不得以说明文字、静态摆设或假按钮代替。
- **当前最终态**：每次修改都把代码直接重构成当前确认的最终设计。删除过时实现，不保留旧版、兼容层、注释掉的方案或补丁式分支；历史只由 Git 保存。

## 组件与设计复用顺序

实现页面前先检查仓库，按以下顺序选择方案：

1. `src/components` 中已有的业务组件和平台组件。
2. 现有页面的组合方式，以及 `src/layouts`、`src/global.less`、`src/assets` 中的布局、样式和素材。
3. `package.json` 已安装版本提供的 TDesign、ECharts 等组件能力。
4. 仅服务于当前页面的局部组合和样式。

除非需求明确要求：

- 不新增、升级或替换依赖。
- 不另造与已有组件重复的组件。
- 不抽取跨页面公共组件。
- 不改变共享 AppShell、全局布局或设计语言。

## 系统主题与样式

- 基础工程全局引入 `src/design-tokens/tdesign-theme.css` 作为系统主题；该文件定义的 TDesign Design Token 是系统颜色、字体、圆角、尺寸、间距和组件视觉样式的统一基准。
- 页面和组件默认使用系统主题、TDesign 组件原生样式及仓库已有样式模式。除非需求明确要求，不自定义颜色，不覆盖或重写系统主题与 TDesign 组件样式。
- 页面仅为业务布局和交互状态添加必要的局部样式，并优先使用系统主题变量，不硬编码已有主题变量能够表达的视觉值。

## 图表与 ECharts

- 图表统一使用仓库当前版本的 `echarts-for-react` 组件和 `echarts` 能力，不使用原生 DOM、Canvas、CSS 或其他图表库模拟。
- 图表必须导入 `src/design-tokens/zhijilab-echarts-theme.json`，并将其中的 `theme` 对象传给 ECharts 组件；该文件是图表颜色、坐标轴、图例、提示、缩放等通用视觉样式的唯一基准。
- 页面只在 ECharts `option` 中表达当前需求的数据、图表类型、业务文案和必要布局。除非需求明确要求，不覆盖主题中已有的颜色与通用视觉配置，也不在页面内复制维护另一份图表色板。

## 页面与路由约定

- 以“页面”为隔离和交付单位，不按一次需求创建独立 App。
- 页面位于 `src/pages/<code-path>/`，不同页面之间禁止互相导入。
- 页面 URL 与 `src/pages` 下的目录路径完全一致。例如 `src/pages/prompt/bot/index.tsx` 对应 `/prompt/bot`。
- `src/App.tsx` 自动发现页面与元数据；禁止手写或维护第二份路由表、菜单表。
- 页面只实现主内容区，统一运行在 `src/layouts/PlatformLayout.tsx` 提供的 AppShell 中。页面不得重复实现顶部栏、一级菜单、二级菜单或后台框架。
- 原型页面需要获取页面上下文时，从 `@/prototypeContext` 使用 `useCurrentBusiness()` 读取当前业务的 `project_id`、`game_id`，使用 `useCurrentUser()` 读取当前用户的 `user_id`、`avatar`、`user_name`；这些上下文均为 Mock，仅供原型页面使用。

每个页面目录使用以下结构：

- `index.tsx`：必需，页面入口。读取预览场景、装配 Mock 数据、持有跨区域共享的状态，并编排 `components/` 中的页面组件。
- `meta.ts`：必需，声明页面菜单。
- `scenarios.ts`：必需，声明可供评审切换的页面预览场景。
- `README.md`：必需，记录页面功能、需求内容和需要传递给开发者的信息。
- `components/`：页面从 `index.tsx` 拆出的组件全部放在这里。
- `mock.ts`：仅在页面需要 Mock 数据或 Mock 操作时创建。
- `types.ts`：仅在业务类型较多、从页面实现分离后更清晰时创建。
- `index.module.less`：仅在页面确有局部样式时创建。

## 页面内组件拆分

`index.tsx` 是页面入口，不是页面的全部实现。页面内容超出单文件可读范围时，按业务区域拆成组件放入同目录 `components/`，不把全部 UI、状态和交互堆在 `index.tsx`。

- 单个文件超过 500 行，或单个组件函数超过 200 行，必须继续拆分。`index.tsx` 通常应控制在 300 行内。
- 禁止用 `renderXxx` 闭包函数代替组件。需要独立的一块界面就拆成组件，通过 props 接收数据和回调。
- 按业务区域和职责边界拆分，例如列表区、详情区、业务流程中的单个步骤、独立弹窗、图表区块。不按 hooks、utils、views 等技术分类拆分，也不为了压低行数把连续的渲染逻辑硬切成互相依赖的碎片。
- 只服务于单一区域的状态、派生值和事件处理下沉到对应组件内部；需要跨区域共享或参与流程判断的状态留在 `index.tsx`，通过 props 和回调传递。
- 组件文件使用 PascalCase 命名并默认导出同名组件，例如 `components/IterationBoard.tsx`。`components/` 及其子目录中禁止出现 `index.tsx`，该文件名只用于页面入口。
- 组件 props 使用显式 TypeScript 接口声明。组件需要预览场景时由 props 传入，不在页面内组件中重复调用 `usePageScenario`。
- 每个组件用 JSDoc 写一句功能描述，props 接口的每个字段用 JSDoc 单行说明含义。只写职责、业务含义和约束，不复述实现细节，不写变更说明。
- 页面内组件只能被同一页面目录内的文件导入。确需跨页面复用时，按“组件与设计复用顺序”放入 `src/components`，不在页面之间互相导入。

拆分只做组件划分：不引入 Controller、ViewModel、Store、固定的 `logic.ts` 分层或页面级自定义 Hook 层。保持 Mock、必要的复杂类型与页面实现边界清楚，同时优先保持简单。

## 页面预览场景

- 每个页面必须在同目录的 `scenarios.ts` 中使用 `definePageScenarios` 声明一组具有业务含义、互斥且可稳定复现的预览场景。至少包含正常默认场景，并覆盖本页适用的加载、空数据、筛选无结果、错误、无权限、边界数据、弹窗或表单校验等关键状态；不机械添加与页面无关的状态。
- 页面通过 `usePageScenario` 获取当前 `previewScenario`，由场景决定 Mock 数据、请求结果和交互初始状态。禁止使用多个布尔变量拼接出相互冲突的页面状态，也禁止为不同场景复制页面实现。
- 预览场景只设置页面的确定性初始状态。切换场景后由工程统一重置页面本地状态；进入任一场景后，需求定义的真实交互仍需使用真实组件并形成闭环，不得用截图、说明文字或仅供展示的静态分支代替。
- 全局 AppShell 自动提供统一的原型工具入口，工具面板内包含页面状态切换和 README 入口，并通过 `?__scenario=<key>` 保持和分享当前场景。页面不得自行实现工具面板、修改其样式或把评审控件混入产品操作区。
- 原型工具是评审基础设施，不属于真实产品内容；除 AppShell 统一提供的工具入口外，页面不得显示原型、Mock、设计或实现说明。
- 页面 `README.md` 必须记录本页已实现的预览场景及其业务含义，确保场景声明、页面实现和交接说明一致。

## 菜单元数据

菜单完全由各页面同目录的 `meta.ts` 提取。`menu` 是一级菜单节点，`child` 是可递归声明的下一级菜单节点：

```ts
import { definePageMeta } from '@/page';

export default definePageMeta({
  menu: {
    label: 'AI 应用',
    icon: aiAppIcon,
    child: {
      label: '应用管理',
      icon: appManagementIcon,
      child: {
        label: '应用列表',
        icon: appListIcon,
      },
    },
  },
});
```

- 一级菜单页面省略 `child`，点击一级菜单直接进入该页面。
- 二级菜单页面使用一层 `child`；三级菜单页面在二级节点内继续使用 `child`。有下级节点的二级菜单作为可展开和收起的目录。
- 各级 `label` 相同的页面自动归入同一菜单组。
- 同一一级菜单组必须使用一致的一级名称和图标。
- 菜单名称和图标必须表达真实产品含义，不出现“原型”“示例”“Demo”等研发语义。

## 页面 README

每个页面的 `README.md` 是页面交付物的一部分，面向后续开发者，并与页面代码一起维护为当前最终态。至少记录：

- 页面名称、访问路径和菜单位置。
- 页面目标、功能范围与主要内容。
- 已确认的需求规则、交互行为、状态变化和关键业务约束。

按实际信息补充原始需求、产品文档、设计稿、接口文档等链接，以及无法仅从原型界面准确传递、但开发实现必须知道的数据、权限、接口或业务规则。

- 只记录本页面已有依据的信息，不推测需求，不补写未来方案。
- 没有相关资料时省略对应内容，不写“待补充”、TODO、空链接或示例链接。
- 原型或需求变更时同步重写 `README.md`，删除已经失效的描述，不保留变更历史。
- 面向用户的产品内容只放在页面 UI；实现交接信息只放在 `README.md`，不得显示到页面中。

## 产品内容与交互

- 页面主内容区只出现真实产品内容和符合业务语义的数据，不展示设计解释、实现说明、Mock 标记或面向开发者的提示；AppShell 统一提供的原型评审基础设施除外。
- 确需向用户解释的字段或操作，复用 `TooltipIcon` 展示简短提示，不使用大段说明文字占据页面。
- Mock 数据应确定、可信并能支撑完整交互；避免随机数据造成评审结果不稳定。
- 需求中的全部交互都要闭环实现，包括必要的状态变化与用户反馈。
- 设计解释仅在确有必要时写入代码注释；开发交接信息写入页面 `README.md`，不要再创建其他说明文档。

## Scope Control

- 只实现用户明确要求的页面、内容和交互。
- 禁止添加未要求的功能、产品入口、按钮、筛选项、业务状态、模块、页面 `README.md` 之外的文档、TODO、未来规划或“顺手优化”。页面预览场景只覆盖本页已经存在或需求必然产生的状态，不借场景设计扩大业务范围。
- 不为了架构完整而增加抽象、兼容层或预留能力。
- 不创建页面生成脚本、baseline 机制或测试用例；只有用户明确要求时才可添加。

## 协作、Git 与发布工作流

- 本工程主要面向非技术用户。沟通时使用产品与业务语言说明结果，避免要求用户理解命令、代码、分支等研发细节；需要用户参与时，直接说明用户要做什么以及预期看到什么。
- 每一轮对话结束时，根据当前进度给出一个明确、可执行的下一步：需求尚未确认时指出需要确认的内容；本地实现完成时提供页面链接和全部预览场景，引导用户逐项验收；验收通过后说明提交与发布动作；推送完成后引导用户等待并使用线上地址复核。
- 本工程不使用多分支开发。所有开发、修改和提交都直接在 `main` 分支进行；开始实现前同步 `origin/main`，但注意妥善处理冲突，避免丢失已有修改。
- 标准交付流程为：确认需求 → 在 `main` 实现 → 执行 `npm run build` → 启动 `npm run dev` 并由用户人工验收全部场景 → 提交并推送到远端 `main` → 自动构建并发布并推送预览地址。

## 完成标准

- 修改后的仓库可直接安装、运行和评审。
- 每个页面的场景声明完整，场景切换器中可逐项进入并看到符合定义的确定性页面状态。
- 至少执行 `npm run build`，修复全部构建和类型错误。
- 视觉与交互由用户在浏览器中人工检查。执行 `npm run dev` 并向用户提供可访问的页面链接，不使用浏览器自动化代替人工检查。
- 发现问题时直接修正到最终态，不把已知问题、未完成项或 TODO 留给交付。

## 注意事项

- 非必要，不要修改工程框架、提示词或根目录的 `README.md`。只有在任务明确要求，且已充分理解修改目的、影响范围和现有约定时，才可以调整上述内容。
